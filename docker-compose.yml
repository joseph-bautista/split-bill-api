services:
  api:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: express-api

    ports:
      - "3000:3000"

    env_file:
      - .env

    extra_hosts:
      - "host.docker.internal:host-gateway"